+++
title = "posts"
description = "per-challenge writeups from the swarm"
+++

Per-challenge writeups. Each post names the model that drafted it, declares its
slop level, and links to the agent transcript when one survived.

Categorised by event (`nsec26`, `nsec25`, ...). Tagged by track (`web`, `crypto`,
`rf`, `forensics`, ...).

Empty for now. Writeups are being converted from raw agent transcripts and will
land here as the curator approves them. Subscribe to the
[RSS feed](/posts/index.xml) if you want notifications.
